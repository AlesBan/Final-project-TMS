@model WebApp_Data.Models.UserDtoLogin

@{
    Layout = "_Layout";
}

<div class="wrapper">
    <h1>Login</h1>
    @using (Html.BeginForm("Login", "Account", FormMethod.Post))
    {
        <form action="#">
            @Html.TextBoxFor(m => m.UserName, new { @type = "text", @placeholder="Username" })
            @Html.TextBoxFor(m => m.Password, new { @class = "form-control", @placeholder="Password" })
        </form>
        <div class="terms">
            <button class="wrapper-SignUp">Login</button>
            <div class="member">
                Not a member? <a href="@Url.Action("SingUp", "Account")">Register Now</a>
            </div>
        </div>
    }
</div>